# Self-hosted typefaces

The storefront uses the Latin variable builds of Jost and Cormorant Garamond. The files were sourced from Fontsource 5.3.0 and are distributed under the SIL Open Font License included in this directory.

The critical files are preloaded in `index.html`; their `@font-face` declarations live in `src/index.css`.
